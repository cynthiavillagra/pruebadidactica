# ===========================================================================
# .env.example - Template de Variables de Entorno
# ===========================================================================
# INSTRUCCIONES:
# 1. Copiar este archivo a .env: copy .env.example .env
# 2. Reemplazar los valores placeholder con tus credenciales reales
# 3. NUNCA subir .env al repositorio (ya esta en .gitignore)
# ===========================================================================

# ---------------------------------------------------------------------------
# SUPABASE - Credenciales (OBLIGATORIO)
# ---------------------------------------------------------------------------
# Obtener de: https://app.supabase.com -> Settings -> API

# URL del proyecto Supabase
SUPABASE_URL=https://tu-proyecto-id.supabase.co

# API Key publica (anon) - Protegida por RLS
SUPABASE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.tu-anon-key-aqui

# JWT Secret - SOLO para validacion backend (Settings -> API -> JWT Secret)
SUPABASE_JWT_SECRET=tu-jwt-secret-aqui

# ---------------------------------------------------------------------------
# FLASK - Configuracion del Servidor
# ---------------------------------------------------------------------------

# Entorno: development | production
FLASK_ENV=development

# Modo debug (1 = activado, 0 = desactivado)
FLASK_DEBUG=1

# Secret key para sesiones Flask (generar con: python -c "import secrets; print(secrets.token_hex(32))")
FLASK_SECRET_KEY=genera-una-clave-secreta-aqui-con-32-caracteres-min

# Puerto del servidor local
PORT=5000

# ---------------------------------------------------------------------------
# SEGURIDAD - Configuracion de Sesion
# ---------------------------------------------------------------------------

# Tiempo de inactividad antes de cerrar sesion (en segundos)
# 15 minutos = 900 segundos
SESSION_TIMEOUT_SECONDS=900

# ===========================================================================
# NOTAS DE SEGURIDAD
# ===========================================================================
# - Este archivo (.env.example) SI se sube al repo (es template)
# - El archivo .env con valores reales NUNCA se sube
# - Las credenciales de Supabase estan en el dashboard del proyecto
# - El JWT Secret es sensible, no compartir
# ===========================================================================
